apiVersion: v1
kind: Service
metadata:
  name: {{ template "helm-sso.fullname" . }}
  labels:
    app: {{ template "helm-sso.name" . }}
    chart: {{ template "helm-sso.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "helm-sso.name" . }}
    release: {{ .Release.Name }}
